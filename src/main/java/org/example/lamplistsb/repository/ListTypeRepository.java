package org.example.lamplistsb.repository;

import org.example.lamplistsb.entity.ListType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ListTypeRepository extends JpaRepository<ListType, Integer> {
}